spring:
  datasource:
    url: jdbc:mysql://localhost:3306/automobile?createDatabaseIfNotExist=true
    username: root
    password: root
  application:
    name: automobile
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQL8Dialect
server:
  servlet:
    contextPath: /api
springdoc:
  swagger-ui:
    path: /swagger-ui

jwt:
  secret: k205j@dkjf0%^@
  expiration: 330000