package com.automobile.pumba.data.transfer.model;

public enum UserRoleGroupB {

    OPERATOR(UserRole.OPERATOR),
    DEALER(UserRole.DEALER);

    UserRoleGroupB(UserRole correspondingRole) {
    }
}